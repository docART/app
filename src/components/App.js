import React from 'react';
import PrototypeList from './PrototypeList';

const App = () => {
    return (
        <div>
            <h2>Prototipos</h2>
            <PrototypeList />
        </div>
    );
};

export default App;
